import { Route,Routes,Navigate } from "react-router-dom";import Home from "./routes/Home";import Banking from "./routes/Banking";import Admin from "./routes/Admin";import News from "./routes/News";import Jobs from "./routes/Jobs";import NotFound from "./routes/NotFound";import {useAuth} from "./lib/auth";import {useState} from "react";import {post} from "./lib/api";import {Button} from "./components/ui/button";import {Input} from "./components/ui/input";import {Card,CardContent,CardHeader,CardTitle} from "./components/ui/card";import VillageShell from "./components/VillageShell";
const adminPaths=["/admin","/admin/users","/admin/accounts","/admin/deposits","/admin/deposits/:id","/admin/withdrawals","/admin/withdrawals/:id","/admin/transfers","/admin/transfers/:id","/admin/transactions","/admin/diamond-reserves","/admin/economy","/admin/economy/charts","/admin/balance-history","/admin/news","/admin/news/:id","/admin/jobs","/admin/jobs/:id","/admin/job-applications","/admin/audit-log","/admin/system"];
export default function App(){return <Routes><Route path="/" element={<Home/>}/><Route path="/dashboard" element={<Home/>}/><Route path="/profile" element={<Profile/>}/><Route path="/deposit" element={<Banking/>}/><Route path="/withdraw" element={<Banking/>}/><Route path="/send" element={<Banking/>}/><Route path="/transactions" element={<Banking/>}/><Route path="/news" element={<News/>}/><Route path="/news/:id" element={<News/>}/><Route path="/jobs" element={<Jobs/>}/><Route path="/jobs/:id" element={<Jobs/>}/>{adminPaths.map(p=><Route key={p} path={p} element={<Admin/>}/>)}<Route path="/marketplace" element={<Navigate to="/dashboard" replace/>}/><Route path="/purchases" element={<Navigate to="/transactions" replace/>}/><Route path="*" element={<NotFound/>}/></Routes>}
function Profile(){const {user,refresh}=useAuth();const [p,setP]=useState("");const [m,setM]=useState("");if(!user)return <Navigate to="/" replace/>;async function change(){try{await post("/auth/change-password",{password:p});setM("Password changed.");setP("");await refresh()}catch(e:any){setM(e.message)}}return <VillageShell><main className="mx-auto max-w-xl p-6"><Card><CardHeader><CardTitle>Profile & security</CardTitle></CardHeader><CardContent className="space-y-4"><p><b>Minecraft username:</b> {user.username}</p><p><b>Role:</b> {user.role}</p><Input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="New password (8+ characters)"/><Button onClick={change}>Change password</Button>{m&&<p className="text-sm">{m}</p>}</CardContent></Card></main></VillageShell>}
